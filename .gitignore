# Public meta-repo: only README (+ this file) ship to GitHub.
# Everything else is local workspace or a sibling repo with its own remote.
/*
!/.gitignore
!/README.md

# OS / editor
.DS_Store
Thumbs.db
